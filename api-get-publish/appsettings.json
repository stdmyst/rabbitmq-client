{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "rabbitSettings": {
    "connectionSettings": {
      "hostname": "localhost",
      "port": 5672,
      "username": "guest",
      "password": "guest",
      "virtualHost": "/"
    },
    "exchange": "test-exchange",
    "queueName": "test-queue"
  }
}
